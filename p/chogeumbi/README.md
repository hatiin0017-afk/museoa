# 초금비 프로필 — 2026-09-17 Supabase 연결

## 실행

저장소: `D:\profile\museoa`

```powershell
python -m http.server 8770 --bind 127.0.0.1
```

- 공개 미리보기: http://127.0.0.1:8770/p/chogeumbi/
- 관리 화면: http://127.0.0.1:8770/p/chogeumbi/admin/
- 해시: `#intro`, `#home`, `#outfits`, `#schedule`, `#upbo`
- 이전 `#profile` 주소는 의상 앨범으로 연결됩니다.
- 940×1350 캔버스를 유지하며 작은 화면에서는 같은 비율로 축소합니다.

## 반영 내용

- 사용자 제공 `D:\profile\초금비\main2.png`를 인트로에 사용합니다. 원본 파일은 보존하며, CSS cover와 가로 42% 기준으로 940×1350 세로 크롭합니다. 금비와 팬캐릭터 얼굴을 함께 보여줍니다.
- 메인 히어로 확대, 캐릭터 중앙 배치, 인사말 오른쪽 이동, 소개를 메인으로 통합했습니다.
- 생일 / 데뷔 / MBTI ISTP / 대표 색상을 표시하고 기존 성격 태그·콘텐츠·음악·소속·하단 이동 카드를 제거했습니다.
- 이름 옆 방송국 링크와 하단 SNS 링크를 배치했습니다.
- 행운은 20%씩 충전됩니다. 100%에서 캐릭터 뒤로 초록 도트 하트가 올라온 뒤 0%로 돌아갑니다. 기본 메시지는 “오늘도 금비와 웃는 하루 ♡”입니다.
- 동작 줄이기 환경에서는 하트 애니메이션을 생략합니다.
- 의상 앨범은 로컬 파일 선택, 설명, 0~20px 블러를 지원합니다. 이미지 주소는 내부에서 자동 지정합니다. 확대 보기에서도 블러를 유지합니다. 블러는 연출용이며 원본 보호 기능이 아닙니다.
- 달력은 고정 크기 칩과 초과 개수 표시, 선택 날짜 상세, 선택 날짜가 속한 일~토 주간 목록을 제공합니다. 주간 일정은 고정 크기의 7칸 미니 달력으로 확인합니다. 날짜를 누르면 전체 상세 모달이 열립니다.
- 관리자에서 일정 상단 영문 멘트/제목/설명/휴방 문구/정기 휴방 요일과 메인 소개·행운 메시지를 편집합니다.
- 업보는 시즌·시청자·당첨 항목·남은 수량·준비 상태를 관리하고 닉네임/아이디로 검색합니다.

## 데이터와 운영 연결 상태

프로젝트 `axbmfxhidogarnelfxdx`의 공개 anon 키를 `supabase-config.js`에 연결했습니다. 기본 주소는 localhost를 포함해 운영 DB를 사용합니다. `admin/setup.sql` 실행과 관리자 계정 등록이 필요하며, 초기 설정 전에는 해당 상태를 화면에 표시합니다.

- `admin/`: 이메일/비밀번호 로그인 → 관리자 명단 확인 → 서버 데이터 편집.
- `admin/?preview=sample&saved=1`: localhost에서만 기존 로컬 데이터 편집. 운영 서버에 쓰지 않습니다.
- `?preview=sample#schedule`: 로컬의 고정 샘플 표시. `&saved=1`을 붙이면 로컬 관리자에서 저장한 데이터를 표시합니다.
- 운영에서는 예시를 자동 생성/이전하지 않습니다. 일정·업보 예시 추가는 관리자가 명시적으로 누를 때만 저장됩니다.
- 서버 저장은 관리자 검사와 revision 비교를 수행합니다. 다른 창이 먼저 저장했으면 덮어쓰지 않고 오류를 표시합니다. 실패 시 기존 데이터와 입력을 유지합니다.
- 공개 RPC는 설정·일정·의상·공개 업보 항목만 반환합니다. 전체 시청자 명부와 처리 이력은 관리자만 읽습니다.
- 공개 페이지는 처음 열 때, 다시 포커스될 때, 활성 탭에서 60초마다 데이터를 갱신합니다.
- 로그인 세션은 탭의 sessionStorage에 저장합니다. 비밀번호/서비스 키는 코드나 localStorage에 저장하지 않습니다.

### 최초 설정

1. Supabase SQL Editor에서 `admin/setup.sql` 전체 실행.
2. Authentication → Users에서 관리자 이메일 계정을 생성하고 이메일 인증 상태 확인.
3. SQL 하단의 관리자 등록 문장을 실제 이메일로 바꿔 실행. 등록 조회 결과가 1행인지 확인.
4. `admin/`에서 그 계정으로 로그인하고 저장/새로고침/이미지 업로드 확인.

기존 `storage-setup.sql`은 이전 이미지 전용 준비안입니다. 신규 설정은 **setup.sql만** 사용합니다. 기존 다른 프로필과 공용 설정 파일은 수정하지 않습니다.

## 확인한 항목

- JS 구문 검사, Git 공백 오류 검사.
- 940×1350 메인/인트로/일정/앨범/업보 화면 확인.
- 390px 화면의 가로 넘침 없음, 390px까지 캔버스 축소 확인.
- 행운 클릭 20% → 100%, 하트 20개 생성, 캐릭터 영역 내부 스크롤 0 확인.
- 관리 멘트 저장, 같은 날 두 일정 등록 → 달력 `+1개` 및 상세/주간 목록 반영.
- 의상 블러 5px 저장 → 공개 카드 및 확대 보기 유지, Escape 닫기.
- 업보 아이디 검색, 업보 상태 변경 → 공개 반영 및 새로고침 후 유지.
- 확인한 공개 페이지 콘솔 경고/오류 없음.

미검증: 운영 로그인·권한·DB CRUD, 타 PC 동기화, SOOP 실제 임베드, 파일 업로드/백업 복원 전체 회귀, 운영 배포.

## GFX 문구 / 샘플 보기

메인 소개의 카드 배경을 제거하고 크기·외곽선·그림자를 조합한 수평 GFX 타이포그래피로 변경했습니다. 문구별 부유/마우스 반응/호버 강조를 적용하며 동작 줄이기 환경에서는 정지합니다.
`?preview=sample#schedule` 또는 `?preview=sample#upbo`는 로컬에서 저장 데이터와 별개로 일정 7건·업보 6건을 표시합니다. 저장 데이터는 덮어쓰지 않습니다.

## 관리자 확장 (2026-09-17)

- 냔냐 공개 관리자와 `js/upbo-admin.js`의 시청자/항목/시즌 배정 구조를 직접 확인했습니다. 참조 파일은 저장소 밖 `D:\profile\nyanya-admin-reference.html`, `D:\profile\nyanya-upbo-reference.js`에 있습니다.
- 시청자(닉네임+아이디), 업보 종류 사전 저장. 시청자/시즌 선택 후 종류별 수량을 클릭 누적합니다.
- 같은 시청자+종류+시즌은 한 배정에 누적하며 시즌이 다르면 별개로 관리합니다.
- `+1`, `1개 처리`, `준비완료/취소`, 누적·처리·남은 수량과 수량 변경 이력을 제공합니다. 기존 데이터에는 확인할 수 없는 과거 처리 이력을 만들지 않습니다.
- 업보 직접 입력 폼은 제거했습니다. 시청자·종류를 각 탭에서 먼저 저장하고 배정 탭에서 누적합니다. 실제 완료는 처리 버튼으로 기록합니다.
- 분류 추가/색상 수정 시 같은 분류의 기존 일정 색상도 갱신됩니다. 시간은 00:00~23:50의 10분 단위, 기본 19:00. 휴방은 시간 없음으로 저장합니다.
- 의상 위/아래 이동, 4개씩 페이지네이션. 샘플 모드에는 실제 새 의상이 아닌 기존 원본을 재사용한 9개 배치 예시가 있습니다.
- 940×1350 검증: 페이지네이션 하단 y=1222, 푸터 시작 y=1273. 1/3~3/3에서 동일합니다.
- 도메인 테스트: `node tests/chogeumbi-model.cjs` (누적/처리/초과 처리 거부/시즌 분리/직접입력 병합/재배정/저장 복원).

### Supabase 이미지 업로드

운영 관리 로그인 계정으로 PNG/JPG/WebP 8MB 이하 파일을 `chogeumbi` 버킷에 업로드합니다. 파일 경로는 `사용자UUID/chogeumbi/파일UUID.확장자`이며, 등록된 관리자 본인 경로만 업로드할 수 있습니다. 파일 선택 후 **의상 저장**을 누르면 업로드와 데이터 연결을 순서대로 처리합니다. 이미지 주소 입력은 숨겼습니다. 삭제 시 다른 의상에서 쓰지 않는 해당 버킷 이미지를 정리하며 실패하면 재시도 항목을 남깁니다. 기존 설치에는 `admin/image-cleanup.sql`을 실행해야 합니다. 운영 데이터에는 base64 이미지를 저장하지 않습니다.

공식 API 참고: [upload](https://supabase.com/docs/reference/javascript/file-buckets-upload), [getPublicUrl](https://supabase.com/docs/reference/javascript/file-buckets-getpublicurl), [RLS](https://supabase.com/docs/guides/database/postgres/row-level-security).

### 연결 구현 검증

- `node tests/chogeumbi-model.cjs`: 누적/처리/시즌 분리/기존 데이터 정규화 통과.
- `node tests/chogeumbi-cloud.cjs`: 원격/로컬 분리, 서버 로드, revision 전달, 저장 실패 시 기존 상태 보존 통과.
- 격리 PGlite PostgreSQL (`D:\profile\qa-pg\test-schema.cjs`): SQL 재실행, 익명 비공개 접근 차단, 비관리자 저장 차단, 공개 필드 제한, 충돌 거부, 업로드 소유자 제한 통과. 실제 Supabase 인증/Storage 검증을 대체하지 않습니다.
- 새 프로젝트 Auth API 응답 확인. 실제 DB 초기 설정 완료 및 공개 RPC 200 응답, 익명 관리자 테이블 접근 거부를 확인했습니다. 관리자 로그인은 사용자 확인 완료. 도구의 브라우저 세션이 공유되지 않아 실제 이미지 업로드 검증은 대기 중입니다.

### 관리자 화면 간소화

- 로그인 완료 후 연결/운영 안내와 초기 SQL 링크는 표시하지 않으며 헤더 우측에는 로그아웃만 표시합니다.
- 업보: 배정·처리 / 시청자 관리 / 업보 종류 / 시즌 관리 / 처리 이력 / 통계. 검색 자동완성 또는 드롭다운으로 시청자를 선택하고 시즌별 내역을 묶어 관리합니다.
- 일정: 일정 등록 / 일정 목록 / 분류 관리. 목록은 월별 조회하며 수정 버튼을 누르면 등록 탭으로 이동합니다.
- 데이터 백업은 별도 탭으로 이동했습니다. 로컬 예시에서는 구분용 안내만 표시합니다.
- 참조: `D:\profile\냔냐\nyanya66-main\admin\index.html`, `js/upbo-admin.js`, `js/schedule-admin.js`의 작업별 탭 구조. 타 프로젝트의 인증/데이터 구조는 복사하지 않습니다.

- 간소화 검증: 작업 탭 전환, 직접 입력 폼 제거, 일정 목록 → 수정 탭 이동 → 저장, 업보 검색 필터, 모바일 가로 넘침 없음 및 콘솔 오류 없음 확인.

### 업보 관리 및 통계 후속 개선

- 선택한 시청자에게 종류 버튼으로 +1 배정, −1 배정 취소, 1개 완료 처리를 제공합니다. 취소는 완료 횟수에 영향을 주지 않습니다.
- 종류별 색상과 삭제를 지원합니다. 삭제한 종류는 신규 배정에서 제외하되 기존 배정과 처리 기록은 보존합니다. 공개 색상은 settings.upboColors로 전달합니다.
- 통계는 시즌·종류·처리 상태·시청자 검색으로 필터링하고 남은 수량 오름차순/내림차순, 완료 수량, 이름순으로 정렬합니다. 총 배정은 현재 남은 수량과 완료 수량의 합이며 취소 수량은 제외합니다.
- 배정 화면과 통계 하단에 시청자별 업보 목록과 관리 이동 버튼을 제공합니다.
- 공개 업보는 SOOP 프로필과 닉네임을 사용한 시청자별 칩으로 표시합니다. 상세 모달은 클릭한 칩 높이에 맞추고 화면 경계를 벗어나지 않도록 조정합니다.
- 로컬 브라우저 검증: 시청자 검색·키보드 선택, +1/−1, 통계 필터·정렬·초기화, 실제 좌표 클릭 전후 스크롤 유지와 모달 높이 일치를 확인했습니다.
- 추가 검증: tests/chogeumbi-storage.cjs의 업로드·삭제 실패·공유 이미지 보존·재시도 테스트, 격리 PostgreSQL의 삭제 권한 및 삭제 대기 이미지 재사용 차단 검사 통과. 실제 인증 계정의 Storage 업로드·삭제는 브라우저 세션 분리로 미검증입니다.
### 이미지 설정 · 인트로 문구 · 문의함 (2026-09-17)

- 관리자 설정 탭에서 인트로 이미지, 메인 캐릭터, 메인 배경 파일을 각각 저장하거나 기본값으로 복원합니다. 운영은 기존 Storage 업로드 권한을 사용하고 URL은 내부 처리합니다. 새 캐릭터 이미지는 전체가 들어오도록 contain으로 표시합니다.
- 멘트·소개 하단의 인트로 히어로에서 상단 좌우 문구, 작은 소개, 제목 두 줄, 입장 버튼 문구를 편집합니다.
- 공개 업보 제목 옆 하트를 문의 버튼으로 교체했습니다. 닉네임과 내용만 접수하며 관리자 업보 → 문의함에서 조회·확인·처리 완료로 관리합니다. 20건 페이지네이션과 상태 필터가 있습니다.
- 문의 설치: 기존 setup.sql과 별도로 admin/inquiries.sql 전체 실행. 사용자 실행 확인 후 실제 RPC의 입력 검증 응답과 익명 조회 차단을 확인했습니다. 문의 내용은 공개 데이터 RPC와 분리됩니다.
- 문의 권한 테스트: NODE_PATH에 @electric-sql/pglite 설치 경로를 지정한 뒤 node tests/chogeumbi-inquiries.cjs. 익명 직접 접근 차단, 접수 RPC, 길이 검사, 같은 닉네임 1분 제한, 관리자 전용 조회·상태 수정, 내용 변조 차단을 검사합니다.
- 시즌 이름 수정은 해당 시즌의 배정·처리 이력 이름을 함께 변경합니다. 시즌 삭제는 확인 후 해당 시즌의 배정과 이력을 함께 삭제합니다.
- 시청자 삭제는 연결된 업보 배정도 삭제하되 처리 이력은 보존합니다. 개별 업보 삭제는 해당 배정만 집계에서 제외하며 이력을 보존합니다. 업보 종류 삭제는 기존처럼 신규 배정에서 제외합니다.
- 선택 시청자 업보는 데스크톱 2열, 모바일 1열로 배치합니다.
- 로컬 UI 검증: 이미지 3종 선택·저장·공개 반영·기본값 복원, 인트로 문구 저장, 문의 접수→문의함→처리 완료, 시즌 이름 수정, 2열 카드 및 콘솔 오류 없음 확인. 운영 Storage의 실제 인증 업로드는 별도 미검증입니다.

### 업보별 시청자·일괄 배정 후속 개선

- 배정·처리 하단의 전체 시청자 업보 / 업보별 시청자 영역은 기본 접힘이며 제목으로 펼칩니다.
- 전체 시즌 선택 시에도 업보 종류 + 시즌으로 그룹을 분리하고 제목 옆에 시즌 배지를 표시합니다.
- 업보별 시청자 내에서 −1 차감(배정 취소), 1개 처리(완료 기록), 해당 배정 삭제를 지원합니다. 다른 시즌의 배정은 유지합니다.
- SOOP 아이디 일괄 배정: 업보와 시즌을 고르고 줄바꿈·쉼표·공백으로 아이디 1~100개 입력. 대소문자 무시 중복 제거, 3개씩 프로필 조회, 성공한 시청자 등록/닉네임 갱신 후 각각 1개 배정. 기존 배정에는 누적합니다. 실패한 아이디는 입력란에 남깁니다.
- 조회 결과는 공통 commit에서 한 번 저장하며 충돌·실패 시 입력을 보존합니다. 관리자 model.js 로드에 버전 파라미터를 붙여 새 일괄 배정 함수의 캐시 불일치를 방지합니다.
- 검증: 실제 SOOP 공개 프로필 조회 + 로컬 예시 저장, 동일 아이디 대소문자 중복 제거, 실패 아이디 보존, 같은 종류의 시즌별 그룹 분리, 목록 내 처리 후 갱신, 기본 접힘, 콘솔 오류 없음. 모델 테스트에서 기존 수량·완료·메모 보존 및 시즌 분리 검사 통과.

### 공개 업보 아이디 검색 방식 변경

- 공개 시즌 선택과 전체 시청자 칩 목록·상세 모달을 제거했습니다. 검색 전에는 검색 폼만 표시합니다.
- SOOP 아이디를 입력하고 검색/Enter를 누르면 대소문자·앞뒤 공백을 정규화한 정확한 아이디 일치로 조회합니다. 닉네임/부분 아이디 검색은 지원하지 않습니다.
- 결과는 프로필·닉네임 아래에 업보명, 시즌명, 상태, 남은 수량을 직접 표시합니다. 완료 수량 0 항목은 제외하고 검색어를 수정하면 기존 결과를 숨깁니다.
- 내역이 많으면 결과 영역 내부 스크롤을 사용해 고정 캔버스·푸터를 유지합니다.
- 검증: 최초 목록 숨김, 부분 아이디 불일치, 대문자 아이디 검색, 시즌 표시, 검색어 삭제 후 숨김, 390px 가로 넘침 없음, 콘솔 오류 없음.
- 검색은 공개 화면 표시 방식이며 본인 인증/비공개 접근 제어가 아닙니다. 기존 공개 RPC 데이터 구조와 관리자 권한은 변경하지 않았습니다.

### 행운 과정·결과 멘트 (2026-09-17)
- 멘트·소개 → 행운받기 멘트에서 과정(20~80%)과 결과(100%)를 각각 관리합니다.
- 각 영역은 문구 추가·수정·삭제, 고정/랜덤 출력 방식, 고정 문구 선택, 저장 전 결과 미리보기를 지원합니다. 최소 1개/최대 30개, 문구당 65자입니다.
- 설정은 `settings.luck.click` / `settings.luck.complete`에 각각 `{messages, mode, fixedIndex}`로 저장합니다. 기존 설정은 과정의 `luckMessage` 및 기존 완료 문구를 유지합니다. 추가 SQL은 필요 없습니다.
- `node tests/chogeumbi-luck.cjs`: 두 모드의 독립성, 선택 및 기존 데이터 호환 검증. 로컬 UI에서 과정 A/B 랜덤 → 100% 결과 고정 출력과 저장을 확인했습니다.

### 일정 업커밍 · 모달 중앙 · 업보 통합 검색 (2026-09-17)

- 일정 하단 주간 미니 달력(WEEKLY SCHEDULE)을 **UPCOMING**으로 교체했습니다. 오늘 포함 앞으로의 일정을 날짜·시간 순으로 최대 3행 표시합니다. 각 행은 `월/일 | 요일 | 시간 | 분류 | 제목 | 내용` 6칸 그리드이며 왼쪽에 분류 색 띠, 오늘 일정에는 배경 강조를 적용합니다. 분류는 색 칩으로 표시하고 테스트 표시는 내용 앞에 붙입니다. 행을 누르면 해당 날짜로 달력을 이동하고 상세 모달을 엽니다.
- 운영 화면의 "수동 등록 일정 · 방송국 공지 자동 연동은 지원하지 않습니다." 문구를 제거하고 그 자리를 업커밍 영역에 넘겼습니다. `#schedule-data-note`는 요소로 남겨 두되 내용이 없으면 숨기며, 로컬 테스트 안내와 동기화 실패 메시지에만 사용합니다.
- 일정 상세 모달을 칩 높이 기준 정렬에서 **화면 정중앙 정렬**로 변경했습니다. 작은 화면에서는 상단 12px 여백을 우선합니다. 사용하지 않게 된 앵커 좌표 보관 변수를 제거했습니다.
- 공개 업보 검색이 **아이디와 닉네임을 모두** 받습니다. 입력 중 부분 일치 후보를 최대 8건 드롭다운으로 보여주며 정확 일치 → 아이디 시작 → 닉네임 시작 순으로 정렬합니다. 마우스 클릭, ↑/↓ 이동 후 Enter, Escape 닫기를 지원하고 후보가 하나면 검색 즉시 선택합니다. 남은 수량이 0인 시청자는 후보에서 제외합니다.
- 940×1350 검증: `#schedule` 하단 y=1273(기존 푸터 기준 동일), 업커밍 영역 y=1016~1245, 3행 고정.
- 검증: 모달 세로·가로 중심 오차 0px, 닉네임 "양갱" 2건·아이디 "sample_" 5건 후보, ↓+Enter 선택 후 결과 표시, 390px 가로 넘침 없음, 콘솔 오류 없음.

### 오버뷰 인라인 메모 · 남은 수량 0 제외 (2026-09-18)

- 배정·처리 하단 **전체 시청자 업보**와 **업보별 시청자** 목록의 각 배정 줄에 비공개 메모(`adminMemo`) 내용을 바로 표시합니다. 시청자 관리로 들어가지 않아도 확인할 수 있습니다. 메모 작성·수정·삭제는 기존처럼 선택 시청자 카드에서만 하며 목록에서는 읽기 전용입니다. 3줄을 넘으면 말줄임하고 전체 내용은 툴팁으로 보여줍니다.
- **전체 시청자 업보**에서 남은 수량이 0인 배정을 제외합니다. 해당 시청자의 모든 배정이 0이면 그룹 자체가 사라집니다. 업보별 시청자는 기존대로 0을 제외합니다.
- 통계 탭의 시청자별 목록은 자체 처리 상태 필터를 쓰므로 0 제외를 적용하지 않습니다. `renderOverview`에 `hideEmpty` 인자를 추가해 배정·처리 호출에서만 켭니다.
- 검증: 샘플 데이터에서 남음 0인 시청자 제외(5그룹), 메모 저장 후 두 목록에 동시 노출, 콘솔 오류 없음.
